# shellcheck disable=SC2168,SC2155
# undefined
builtin autoload -X

#declare -a ARGS=("$@")
#declare FILTER line
#declare dataFile="$(mktemp)"

#if [[ "${#ARGS[@]}" -gt 0 ]]; then
#	FILTER="${1:-}"
#fi

#while IFS= read -r line
#do
#	echo "$line"
#done < "$dataFile"
