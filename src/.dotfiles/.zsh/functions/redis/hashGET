# undefined
builtin autoload -X

if [[ -z "$REDISCLI_AUTH" ]]; then source /"$SWARMDIR"/cfg/.env; fi

(( $# < 2 )) && errorExit "Missing Arguments!"

local value
local key="${1:-}"
local field="${2:-}"

value="$(redis-cli HGET "$key" "$field")"

printf '%s' "$value"
